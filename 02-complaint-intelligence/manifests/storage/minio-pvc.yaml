apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-data
  namespace: complaint-intelligence
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 60Gi   # model artefacts + demo data; size for the 8B model with headroom
