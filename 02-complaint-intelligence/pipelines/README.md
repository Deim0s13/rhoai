Ingestion, embedding and classification pipelines.

Deliberately empty: held until platform validation settles the tracing and
evaluation conventions (docs/architecture.md, open decision 2), so pipeline
instrumentation is written once, not retrofitted.
