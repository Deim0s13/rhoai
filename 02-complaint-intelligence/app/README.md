Thin demo UI: complaint in, classification out, theme-level aggregate view,
review queue for low-confidence cases.

Deliberately empty: held until platform validation settles the tracing and
gateway decisions the app must integrate with.
